# Backups

Timestamped directories (`YYYY-MM-DD_HHMMSS/`) are created by [installer.sh](../installer.sh) when it replaces something in `$HOME`. Those folders are gitignored so a user’s previous config is not committed.

Layout and restore: [README-installer.md](../README-installer.md).
